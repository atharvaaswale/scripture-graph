{
  "entities": {
    "UniversalConstellation": {
      "title": "UniversalConstellation",
      "description": "Universal database containing all verses, edges, and node positions across all devices",
      "type": "object",
      "properties": {
        "verses": {
          "type": "array",
          "description": "List of scripture verses and quotes"
        },
        "edges": {
          "type": "array",
          "description": "Relationships between scripture verses"
        },
        "node_positions": {
          "type": "object",
          "description": "Arranged coordinates (x, y) keyed by node id"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp of the last update"
        }
      }
    }
  },
  "firestore": {
    "/constellation/{docId}": {
      "schema": "UniversalConstellation",
      "description": "Universal shared constellation database across all devices and deployments"
    }
  }
}
